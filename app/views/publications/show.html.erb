<p style="color: green"><%= notice %></p>

<%= render @publication %>

<div>
  <% if user_signed_in?%>
    <%= link_to "Edit this publication", edit_publication_path(@publication) %> |
  <% end%>
  <%= link_to "Back to publications", root_path %>

  <% if user_signed_in?%>
    <%= button_to "Destroy this publication", @publication, method: :delete %>
  <% end%>
</div>
